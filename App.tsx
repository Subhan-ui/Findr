import Navigator from './src/navigation/Navigator';
import Provider from './src/components/provider/Provider';

const App = () => (
  <Provider>
    <Navigator />
  </Provider>
);

export default App;
